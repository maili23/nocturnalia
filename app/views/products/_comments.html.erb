<div class="product-reviews">
  <% @comments.each do |comment| %>
    <div class="row comment-list">
      <hr>
      <p>
      <%= comment.user.first_name %> <small><em><%= "#{time_ago_in_words(comment.created_at)} ago" %></em></small>
        <% if signed_in? && current_user.admin? %>
          <%= awesome_link "fa-times", product_comment_path(comment.product, comment), method: :delete, data: { confirm: '¿En verdad quieres eliminar este comentario?' } %>
        <% end %>
      </p>
      <div class="rated" data-score="<%= comment.rating %>"></div>
      <p><%= comment.body %></p>
    </div>
  <% end %>
</div>